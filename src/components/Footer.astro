<footer class="site-footer">
    <div class="site-footer-inner">
        <p>© {new Date().getFullYear()} Florian Bertholin</p>
        <a
            href="https://github.com/arkan/florianbertholin.com"
            class="site-text-link"
        >
            Source on GitHub
        </a>
    </div>
</footer>

<style>
    .site-footer {
        border-top: 1px solid var(--site-line);
        background: var(--site-surface);
    }

    .site-footer-inner {
        display: flex;
        width: 100%;
        max-width: var(--site-container);
        margin-inline: auto;
        flex-direction: column;
        gap: 0.75rem;
        padding: 1.75rem var(--site-gutter);
        color: var(--site-muted);
        font-size: 0.8125rem;
        line-height: 1.5;
    }

    .site-footer p {
        margin: 0;
    }

    .site-footer a {
        width: fit-content;
    }

    @media (min-width: 640px) {
        .site-footer-inner {
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
        }
    }
</style>
